# ElderAI — Python dependencies
# Install with:  pip install -r requirements.txt

# ── Streamlit ────────────────────────────────────────────────
streamlit>=1.36

# ── Google Gemini (new unified SDK — used everywhere) ────────
google-genai>=1.33.0

# ── LangChain + Gemini bridge ────────────────────────────────
langchain>=0.2.12
langchain-community>=0.2.11
langchain-google-genai>=2.0.0
langchain-text-splitters>=0.2.2

# ── Vector store ─────────────────────────────────────────────
faiss-cpu>=1.8.0.post1

# ── TTS ──────────────────────────────────────────────────────
gTTS>=2.5.3

# ── Image handling ───────────────────────────────────────────
Pillow>=10.4.0

# ── Configuration / secrets ──────────────────────────────────
python-dotenv>=1.0.1

# REMOVED vs previous version:
#   google-generativeai  — old SDK; now using google-genai everywhere
#   langchain-huggingface / sentence-transformers / torch  — dead code (3 GB+)
#   numpy  — transitive dep, not needed explicitly
